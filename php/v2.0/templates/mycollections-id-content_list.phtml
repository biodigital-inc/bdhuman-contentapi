<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Content API /mycollections/{id}/content_list Example</title>

		<style>
			.prettyprint {
				outline: 1px solid #ccc;
				padding: 5px;
				margin: 20px;
				max-height: 200px;
				line-height: 1.2;
				overflow-y: auto;
				overflow-x: auto;
			}
		</style>

	</head>

	<body>

		<p><a href="/">Start Over</a></p>
		<p>Your <b><?=$this->endpoint_path?></b> endpoint response:</p>
		<pre id="output" class="prettyprint"><?=$this->mycollections_id_contentlist_results?></pre>

		<p>Additional team collection endpoints:</p>
		<div>
			<ul style="list-style: none;">
				<li>Return a list of all my team collections:  <a href="?action=query_collection_mycollections&access_token=<?=$this->access_token?>">/mycollections</a></li>
				<?php foreach ($this->mycollections_id_contentlist_results_obj->content_list as $content): ?>
				<li>Return details for this team collection only:  <a href="?action=query_collection_mycollections_id&collection_id=<?=$content->collection_id?>&access_token=<?=$this->access_token?>">/mycollections/<?=$content->collection_id?></a></li>
				<?php break; endforeach; ?>
				<?php if( count($this->mycollections_id_contentlist_results_obj->content_list) == 0): ?>
					<li>No content assoicated with collection</li>
				<?php endif; ?>
			</ul>
		</div>


		<p>Other endpoints:</p>
		<div>
			<ul style="list-style: none;">
				<li>Return a list of all my dashboard content:  <a href="?action=query_collection_myhuman&access_token=<?=$this->access_token?>">/myhuman</a></li>
			</ul>
		</div>

	</body>

</html>